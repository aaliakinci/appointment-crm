pid /tmp/nginx.pid;

events {
  worker_connections 1024;
}
http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;
  access_log /dev/stdout;
  error_log /dev/stderr warn;
  sendfile on;

  map $http_x_forwarded_proto $appointment_forwarded_proto {
    default $http_x_forwarded_proto;
    "" $scheme;
  }

  map $http_x_forwarded_for $appointment_forwarded_for {
    default $http_x_forwarded_for;
    "" $remote_addr;
  }

  client_body_temp_path /tmp/client_temp;
  proxy_temp_path /tmp/proxy_temp;
  fastcgi_temp_path /tmp/fastcgi_temp;
  uwsgi_temp_path /tmp/uwsgi_temp;
  scgi_temp_path /tmp/scgi_temp;

  server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy no-referrer always;

    location ~ ^/(api|health|openapi)/ {
      proxy_pass http://api:8080;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $appointment_forwarded_for;
      proxy_set_header X-Forwarded-Proto $appointment_forwarded_proto;
      proxy_set_header X-Request-Id $http_x_request_id;
    }

    location / {
      try_files $uri $uri/ /index.html;
    }
  }
}
